#include "main.h"
#include <stdio.h>

/**
 * main - calls the function in code block
 *
 * Return: 0
 */

int main(void)
{

	print_line(0);
	print_line(4);
	print_line(6);
	print_line(10);
	print_line(-1);

	return (0);
}
