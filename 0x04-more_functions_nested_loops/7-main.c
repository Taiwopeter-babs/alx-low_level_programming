#include "main.h"
#include <stdio.h>

/**
 * main - calls the function in code block
 *
 * Return: 0
 */

int main(void)
{

	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);

	return (0);
}
